FROM node:16.20.2

WORKDIR /usr/src/app

# Install dependencies
COPY package*.json ./
RUN npm install --legacy-peer-deps

# Copy source
COPY . .

# Expose ports
EXPOSE 3000 5555

# Langsung jalanin, biarin Docker yang atur user default (root)
CMD ["npm", "run", "start:dev"]